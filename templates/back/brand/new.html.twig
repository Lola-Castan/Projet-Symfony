{% extends 'back.base.html.twig' %}
{% block menu_bo_brands %}active-back{% endblock %}
{% block title %}BackOffice - New Brand{% endblock %}

{% block body %}
<main>
    <section class="section-show section-bo">
    <h1 class="bo-title">Create new brand</h1>

    <div class="container p-4">
    <div class="row mb-2 fs-6">
        <div class="col">
            {{ form_start(form, {'attr' : {'novalidate': 'novalidate'} }) }}
            {{ form_row(form.name) }}
            <button class="table-btn">Add the new brand</button>
            {{ form_end(form) }}
        </div>
    </div>
    </div>
    <br>
    <a class="table-btn" href="{{ path('app_brand_index') }}">Back to list</a>
    </section>
</main>
{% endblock %}
